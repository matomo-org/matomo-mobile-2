<Alloy>
    <Window id="index" onClose="onClose">
        <ScrollView class="container">
            <ImageView id="piwikLogo"/>
            <Button id="website" onClick="doOpenWebsite"/>
            <Button id="email" onClick="doEmailUs"/>
            <Button id="follow" onClick="doFollowPiwik"/>
            <Button id="sourceCode" onClick="doOpenSourceCode"/>
            <Button id="license" onClick="doOpenLicense"/>
        </ScrollView>
    </Window>
</Alloy>