<Alloy>
    <Window id="index">
        <NavigationGroup id="navgroup" platform="ios,mobileweb">
            <Window class="container" id="accountView" title="Accounts">
                <TableView id="accounts" onDelete="onDelete" onClick="onSelect" class="accountsContainer" />
                <TableView id="actions" class="accountsContainer">
                    <TableViewRow class="masterPasswordRow">
                        <Label>Master Password</Label>
                        <Button class="masterPasswordInfoButton"></Button>
                    </TableViewRow>
                </TableView>
            </Window>
        </NavigationGroup>
    </Window>
</Alloy>